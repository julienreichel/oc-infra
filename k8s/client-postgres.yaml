apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oc-client-pg-pvc
  namespace: oc-client
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oc-client-pg
  namespace: oc-client
spec:
  selector:
    matchLabels:
      app: oc-client-pg
  template:
    metadata:
      labels:
        app: oc-client-pg
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: oc-client-db-secret
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: oc-client-db-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: oc-client-db-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
              subPath: postgres
      volumes:
        - name: pgdata
          persistentVolumeClaim:
            claimName: oc-client-pg-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: oc-client-pg
  namespace: oc-client
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: oc-client-pg
