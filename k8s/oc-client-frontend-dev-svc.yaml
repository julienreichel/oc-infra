apiVersion: v1
kind: Service
metadata:
  name: oc-client-frontend-dev
  namespace: oc-client
spec:
  type: ExternalName
  externalName: host.k3d.internal
  ports:
    - name: http
      port: 9000
      targetPort: 9000
